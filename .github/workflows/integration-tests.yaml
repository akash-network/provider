---
name: Integration tests

# yamllint disable-line rule:truthy
on:
  workflow_call:

env:
  KUBE_SSH_NODES: kind

defaults:
  run:
    shell: bash

jobs:
  crd-e2e:
    strategy:
      fail-fast: false
      matrix:
        # Each suite runs the shared setup below on its own runner and kind
        # cluster, in parallel. Parallel legs cannot share a live cluster, so
        # the setup recipe is shared, not the environment.
        suite:
          - integration
          - provider-gates
    name: crd-e2e (${{ matrix.suite }})
    # Test execution runs with a read-only token. Publishing the workload-health
    # comment needs pull-requests: write, so it lives in a separate job (below) that
    # runs no test code.
    permissions:
      contents: read
    env:
      KIND_NAME: kube
    runs-on: core-e2e
    steps:
      - name: Cleanup build folder
        run: |
          sudo rm -rf ./* || true
          sudo rm -rf ./.??* || true
      - name: Setup GOPATH
        run: echo "GOPATH=$GITHUB_WORKSPACE/go" >> $GITHUB_ENV
      - name: Ensure GOPATH dirs
        run: mkdir -p ${{ env.GOPATH }}/{src,bin,pkg}
      - name: Checkout codebase
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          set-safe-directory: true
          # Do not leave a token in .git/config for test code to recover.
          persist-credentials: false
          path: go/src/github.com/akash-network/provider
      - name: Install dependencies
        # Shell must explicitly specify the shell for each step. https://github.com/orgs/community/discussions/18597
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config make direnv unzip lz4 wget curl jq pv coreutils libudev-dev
      - name: Detect required Go version
        working-directory: "${{ env.GOPATH }}/src/github.com/akash-network/provider"
        run: |
          toolchain=$(./script/tools.sh gotoolchain | sed 's/go*//')
          echo "GOVERSION=${toolchain}" >> $GITHUB_ENV
      - uses: actions/setup-go@v5
        with:
          go-version: "${{ env.GOVERSION }}"
      - name: Install tools
        run: |
          curl -sfL https://direnv.net/install.sh | bash
          go install github.com/fullstorydev/grpcurl/cmd/grpcurl@latest
      - name: Hook direnv to bash
        run: echo 'eval "$(direnv hook bash)"' >> $HOME/.bashrc
      - name: Direnv allow
        run: direnv allow ${{ env.GOPATH }}/src/github.com/akash-network/provider
      - name: Checkout chain-sdk
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: |
          AKASH_API_VERSION=$(go list -mod=readonly -m -f '{{ .Version }}' pkg.akt.dev/go)
          echo "AKASH_API_VERSION=$AKASH_API_VERSION" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          repository: "akash-network/chain-sdk"
          fetch-depth: 0
          set-safe-directory: true
          # Same as the provider checkout: no token left on disk for test code.
          persist-credentials: false
          path: go/src/github.com/akash-network/chain-sdk
          ref: go/${{ env.AKASH_API_VERSION }}
      - name: Setup chain-sdk
        run: |
          direnv allow ${{ env.GOPATH }}/src/github.com/akash-network/chain-sdk
          cd ${{ env.GOPATH }}/src/github.com/akash-network/chain-sdk
          cat "$GITHUB_ENV" > "$GITHUB_WORKSPACE/env.backup"
          direnv export gha >> "$GITHUB_ENV"
      - name: Run modvendor on chain-sdk
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/chain-sdk
        run: |
          make modvendor
          cat "$GITHUB_WORKSPACE/env.backup" > "$GITHUB_ENV"
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'
      - run: pip install tqdm
      - name: Setup direnv for provider
        run: direnv export gha >> "$GITHUB_ENV"
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
      - name: Fetch kind version from go modules
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: echo "KIND_VERSION=$(go list -mod=readonly -m -f '{{ .Version }}' sigs.k8s.io/kind)" >> $GITHUB_ENV
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Go mod tidy
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: go mod tidy
      - name: Make node scripts executable
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: make chmod-akash-scripts
      - name: Setup kind
        uses: helm/kind-action@v1
        with:
          version: "${{ env.KIND_VERSION }}"
          node_image: "kindest/node:${{ env.KINDEST_VERSION }}"
          cluster_name: "${{ env.KIND_NAME }}"
          config: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube/kind-config.yaml
      - name: Setup run environment
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          direnv allow
          cat "$GITHUB_ENV" > "$GITHUB_WORKSPACE/env_provider.backup"
          direnv export gha >> "$GITHUB_ENV"
          cat "$GITHUB_ENV" > "$GITHUB_WORKSPACE/env_run.backup"
      - name: Configure Kind cluster
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          KUSTOMIZE_INSTALLS=akash-operator-inventory make kube-cluster-setup-e2e-ci
      - name: Setup K8S ingress
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          make kind-k8s-ip
      - name: K8S dump config
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          kubectl config view
      - name: K8S dump cluster info
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          kubectl cluster-info
      - name: K8S dump running pods
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          kubectl get deployments,pods,ingress,svc -A
      - name: K8S wait for operator inventory
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          make kube-deployment-rollout-operator-inventory
        env:
          KUBE_ROLLOUT_TIMEOUT: 360
      - name: Run modvendor on chain-sdk
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: cat "$GITHUB_WORKSPACE/env_run.backup" > "$GITHUB_ENV"
      - name: Run E2E Tests
        if: matrix.suite == 'integration'
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: |
          make test-e2e-integration
      - name: Run provider disruption gates
        if: matrix.suite == 'provider-gates'
        # Non-blocking while it earns trust: a gate failure is surfaced but does not
        # fail the job.
        continue-on-error: true
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        env:
          # Deploy with the PR's base version, recover with the candidate, so the
          # upgrade gate is a genuine cross-version test. Empty on push/cron, where
          # the gate falls back to HEAD (a same-code mechanism check).
          PROVIDER_UPGRADE_BASE_REF: ${{ github.event.pull_request.base.sha }}
        run: |
          set -o pipefail
          make test-e2e-provider-gates 2>&1 | tee "$RUNNER_TEMP/provider-gates.log"
      - name: Render workload health summary
        # Shown in the job summary (always, including push/cron with no PR) and handed
        # to the publish job via an artifact so this read-only job never needs the
        # write token.
        if: always() && matrix.suite == 'provider-gates'
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: |
          bash script/provider-gates-summary.sh "$RUNNER_TEMP/provider-gates.log" \
            > "$RUNNER_TEMP/provider-gates-summary.md"
          cat "$RUNNER_TEMP/provider-gates-summary.md" >> "$GITHUB_STEP_SUMMARY"
      - name: Upload workload health summary
        if: always() && matrix.suite == 'provider-gates' && github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: provider-gates-summary
          path: ${{ runner.temp }}/provider-gates-summary.md
          retention-days: 1
          # Immutable per run; allow "re-run failed jobs" to replace it instead of 409.
          overwrite: true
      - name: Print operator inventory logs
        if: always()
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          kubectl -n akash-services logs -l app.kubernetes.io/part-of=provider,app.kubernetes.io/component=operator,app.kubernetes.io/instance=inventory-service,app.kubernetes.io/name=inventory
      - name: Print operator inventory discovery logs
        if: always()
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          kubectl -n akash-services logs -l app.kubernetes.io/part-of=provider,app.kubernetes.io/component=operator,app.kubernetes.io/instance=inventory-hardware-discovery,app.kubernetes.io/name=inventory
      - name: Run K8s Tests
        if: matrix.suite == 'integration'
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider
        run: |
          make test-k8s-integration
      - name: Post-Run K8S environment
        working-directory: ${{ env.GOPATH }}/src/github.com/akash-network/provider/_run/kube
        run: |
          kubectl get ns,pods,ingress,svc -A

  # Publishing the workload-health comment is the only step that needs a write-scoped
  # token, so it runs in its own job that executes no test code, consuming the summary
  # the test job produced as an artifact.
  publish-gates-summary:
    name: Publish workload health comment
    needs: crd-e2e
    # Same-repo PRs only: a fork PR's GITHUB_TOKEN is read-only regardless of the
    # permissions block, so it cannot comment. Forks still see the table in the job
    # summary; skipping here avoids a guaranteed 403 failure.
    if: always() && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Download workload health summary
        id: download
        uses: actions/download-artifact@v4
        with:
          name: provider-gates-summary
          path: gates-summary
        continue-on-error: true
      - name: Post workload impact to PR
        # One sticky comment per PR, updated in place. gh api --paginate walks every
        # comment page, so the marker is found even on long PRs (>100 comments).
        if: hashFiles('gates-summary/provider-gates-summary.md') != ''
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          marker='<!-- provider-gates-summary -->'
          body=$(printf '%s\n%s\n' "$marker" "$(cat gates-summary/provider-gates-summary.md)")
          # `|| true` so head closing the pipe early does not SIGPIPE-fail gh under
          # the workflow's `set -eo pipefail`.
          id=$(gh api --paginate "repos/$REPO/issues/$PR/comments" \
            --jq ".[] | select(.body | contains(\"$marker\")) | .id" | head -n1 || true)
          if [ -n "$id" ]; then
            gh api -X PATCH "repos/$REPO/issues/comments/$id" -f body="$body" >/dev/null
          else
            gh api -X POST "repos/$REPO/issues/$PR/comments" -f body="$body" >/dev/null
          fi
